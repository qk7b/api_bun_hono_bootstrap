meta {
  name: refresh
  type: http
  seq: 4
}

post {
  url: {{host}}/auth/refresh
  body: json
  auth: none
}

body:json {
  {
    "token": "{{bearer}}"
  }
}

script:post-response {
  var token = res.body.token;
  bru.setEnvVar("bearer", token);
}

settings {
  encodeUrl: true
}
